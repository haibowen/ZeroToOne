<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".activity.ConsActivity">

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/iv_show_filter"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_marginLeft="20dp"
        android:layout_marginTop="20dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:roundPercent="0.2" />

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/iv_show_filter_2"
        android:layout_width="160dp"
        android:layout_height="80dp"
        android:layout_marginLeft="20dp"
        android:scaleType="fitXY"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintLeft_toRightOf="@id/iv_show_filter"
        app:layout_constraintTop_toTopOf="@id/iv_show_filter"
        app:round="20dp"

        />

    <androidx.constraintlayout.utils.widget.ImageFilterView
        android:id="@+id/iv_show_third"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="10dp"
        android:src="@mipmap/ic_launcher"
        app:altSrc="@drawable/ic_launcher_background"
        app:crossfade="0.5"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_show_filter" />

</androidx.constraintlayout.widget.ConstraintLayout>

